# 交接提示词（复制粘贴给新会话的 AI）

你是在 `E:\dsharness\新控制代码` 目录下工作的控制代码调试助手。这是一个雷达阵面驱动机构的控制代码（调平 + 方位 + 举升/俯仰 + MPC），本目录是从 `E:\dsharness\控制代码` 复制出的调试工作副本。

## 先读这些（严格遵守）

- 目录内 `CLAUDE.md`：运行环境坑 + 位置驱动现状 + 验证命令
- 目录内 `AGENTS.md`：项目安全规则 + 编码/单位约定

## 运行环境（不要违反）

1. 只用 `.venv\Scripts\python.exe` 运行，**不要用 `python`**（WindowsApps 别名已损坏，返回退出码 49），**也不要 `py`**（系统 Python 缺 `osqp`）。
2. `.venv` **不能重建**（`python -m venv` 返回 49），只能整体复制。
3. 本目录不是 git 仓库。

## 已完成的现状

举升缸**位置闭环驱动**已实现并离线验证通过（正向/反向/大行程到位误差 ≤0.002°，卡死保护、软限位钳制正常）：

- `MPC_fixed/lift_position_controller.py`（新增）：纯计算位置环（梯形速度曲线 + 位置 P(I)(D) + 跟随误差保护）
- `MPC_fixed/lift_executor.py`（扩展）：位置驱动接口 `start/update/stop/configure/is_arrived/is_fault`
- `MPC_fixed/lift_07_position_drive.py`（新增）：独立测试窗口（含方向系数输入）
- `MPC_fixed/master_controller.py`（扩展）：IDLE 模式集成位置驱动
- 文档 `docs/lift_position_drive.md`（架构/用法/参数/安全/自测结果）

## 当前任务（按优先级）

1. 真机**方向预检**：小角度（当前角 ±0.3°）慢速（0.3 deg/s）点动，确认 `pitch_lift_direction` 方向正确
2. 真机调参（Kp / 速度 / 死区）
3. 可选增强：位置目标接入 MPC 轨迹、CSV 增加位置目标列、一键判断方向按钮

## 我的当前需求

（在这里填你这次要 AI 做的具体事情）

---

请先读 `CLAUDE.md` 和 `AGENTS.md`，再根据上面的现状和「我的当前需求」开始，不要重复解释环境。
